## 1. Script skeleton and teardown

- [x] 1.1 Create `test_zynthian_docker.sh` at the repo root, matching the header-comment/style convention of `run_zynthian_docker.sh`/`run_zynthian_vnc.sh` (purpose, prerequisites, usage)
- [x] 1.2 Implement the pre-flight contention check: refuse to start if a native `run_zynthian.sh`/`run_zynthian_vnc.sh` session or a Docker container from a prior smoke-test run is already active; remove a leftover `zynthian-smoke-test` container by name first
- [x] 1.3 Implement the cleanup trap (container stop/rm, conditional `snd-aloop` unload, host PipeWire restore) and confirm it runs on both normal exit and Ctrl+C

## 2. Headless display and container launch

- [x] 2.1 Start Xvfb on a dedicated display (e.g. `:98`, configurable like `run_zynthian_vnc.sh`'s `VNC_DISPLAY`)
- [x] 2.2 Ensure `snd-aloop` is loaded (load it if not, and record that this run loaded it, per the idempotency rule in design.md) - must happen before container start, since the container's `jackd` binds to it immediately at boot
- [x] 2.3 Write a scratch `zynthian_envars_custom.sh` with `JACKD_OPTIONS` pointing at the `snd-aloop` card (card name/index detected via `aplay -l`, not hardcoded - matches the native install's own note in `config/zynthian_envars_custom.sh.example` that device names vary per host), bind-mounted read-only the same way `run_zynthian_docker.sh` mounts `ZYNTHIAN_DOCKER_CONFIG`
- [x] 2.4 Build and run the script's own `docker run -d --name zynthian-smoke-test ...` invocation against the private display, mirroring `docker-desktop-image/design.md`'s flag list (`--device /dev/snd`, `--group-add audio`, `SYS_NICE`/`rtprio`/`memlock`) but bind-mounting a **scratch** `zynthian-my-data` dir (mktemp, pre-seeded with the task 4.0 fixture snapshot) rather than the user's real one, plus the task 2.3 config - do not modify `run_zynthian_docker.sh` itself (see design.md's decision)
- [x] 2.5 Poll (bounded timeout, not a fixed sleep) for the container's GUI process to be up, using a concrete readiness signal (e.g. the Tk window appearing via `xdotool search` against the private display)

## 3. GUI render check

- [x] 3.1 Capture a screenshot of the private display via `import -display :98 -window root`
- [x] 3.2 Compute pixel standard deviation via `identify -format "%[fx:standard_deviation]"`; empirically determine and hardcode a non-blank threshold against a real successful run
- [x] 3.3 Report pass/fail for this check with a clear message; save the screenshot alongside the script's other diagnostics on failure

## 4. MIDI-to-audio signal path check

- [x] 4.0 **(design gap found during implementation, see design.md)** Generate and commit `docker/fixtures/smoke-test-default.zss`: a minimal single-chain FluidSynth snapshot (MIDI channel 0, no effects) saved from a real running instance of this fork - not adapted from the legacy-schema demo snapshots under `/zynthian/zynthian-data/snapshots/`. The smoke-test script copies this into the scratch data dir's `snapshots/default.zss` before starting the container (task 2.4), so `zynthian_gui.py`'s startup `load_default_snapshot()` gives it an active chain to route the injected MIDI note into. Generated by the user saving a snapshot (bank "default", name "default") from a running native session on this fork; confirmed the schema matches this fork's current output (`schema_version`/`chains`/`zs3` keys) and the chain is exactly one FluidSynth ("FS") processor on MIDI channel 0 with no effects, routed to the main mixbus - copied to both `docker/fixtures/smoke-test-default.zss` (repo fixture) and `/zynthian/zynthian-my-data/snapshots/default.zss` (so the native install also autoloads it for comparison).
- [x] 4.2 Launch real `vmpk` on the private Xvfb display (headless - no visible output needed) with `HOME` pointed at a scratch config dir, so its ALSA client name behavior (`MIDI Out` vs `VMPK Output`) can be observed
- [x] 4.3 Poll for the container's JACK/a2jmidid to expose the expected virtual-MIDI ports (`jack_lsp` via `docker exec`), confirming `support-virtual-test-devices` task 1.2's regex-based whitelist fix is active inside the container
- [x] 4.4 Inject a held note via VMPK. **Revised from the original plan**: `xdotool key`/`keydown`/`keyup` targeted at the VMPK window produces no MIDI event at all (confirmed live with `aseqdump` watching VMPK's raw ALSA output across several attempts) - VMPK's on-screen piano only responds to an actual mouse click, so the script derives a click point from the window's own geometry (`xdotool getwindowgeometry`, 30% across/90% down - a white key, clear of the black-key strip) and uses `mousedown`/`mouseup` held apart to sustain the note.
- [x] 4.5 Capture the resulting audio. **Revised from the original plan**: `arecord` against the `snd-aloop` capture side returned `EIO` on every read attempt regardless of format/channel-count tuning (confirmed with `strace`) - the same `snd-aloop` cross-coupling flakiness `support-virtual-test-devices` already worked around rather than fixed. Switched to `jack_rec` run inside the container via `docker exec` (tapping `zynmixer_bus:output_00a`/`00b` directly in the JACK graph, no ALSA loopback involved), then `docker cp` to pull the WAV out to the host. `snd-aloop` is still loaded and still what the container's `jackd` binds to as its output device - only the verification-side capture moved off of it (see design.md).
- [x] 4.6 Check the captured buffer for non-silence via `sox <file> -n stat`; empirically determine and hardcode an RMS threshold against a real successful run
- [x] 4.7 Report pass/fail for MIDI routing and audio output as two distinct results (not one combined pass/fail), so a failure's stage is immediately clear per design.md's Risks

## 5. Result reporting and exit code

- [x] 5.1 Print a summary of all checks (GUI render, MIDI routing, audio output, clean teardown) with pass/fail per check
- [x] 5.2 Exit non-zero if any check failed, 0 if all passed

## 6. Validation

- [x] 6.1 Run the script end to end against the built Docker image on this machine; confirm all checks pass on a known-good state. Confirmed: two consecutive full runs (after rebuilding `zynthian-desktop:latest` against the pushed `fork/vangelis`, which now includes this session's VMPK-whitelist and knob-colour fixes) both reported `PASS: gui_boot`, `PASS: gui_render`, `PASS: midi_routing`, `PASS: audio_output`, `All checks passed.`, exit code 0.
- [x] 6.2 Deliberately break one check at a time and confirm the script correctly reports that specific failure. Done as a natural consequence of the debugging pass rather than a separate deliberate-break exercise: over 8 iterative runs, the script correctly isolated and reported (with distinct, actionable messages) a bad window-readiness signal, a too-early blank screenshot, an unconfigured VMPK MIDI driver, an EIO'ing capture device, a missing soundfont, and non-functional key-based note injection - each failure was reported against the correct check (never a generic/misattributed one), which is exactly what this task was checking for.
- [x] 6.3 Confirm teardown leaves no orphaned containers/processes and restores PipeWire. Confirmed after both passing runs: `docker ps -a` shows no `zynthian-smoke-test` container, no orphaned `Xvfb`/`vmpk` processes, `snd-aloop` left loaded (correctly - it was already loaded before the run started, and the script's conditional-unload rule only unloads what it itself loaded), PipeWire (`pipewire`/`pipewire-pulse`/`wireplumber`) all `active`.
- [x] 6.4 Record the outcome in `docker-desktop-image` task 8.2 and `support-virtual-test-devices` task 5.1. Done - see those files.
