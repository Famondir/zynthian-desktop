## 1. Style selection mechanism

- [x] 1.1 Add `ZYNTHIAN_GUI_KEYPAD_STYLE` env var (`classic`/`standard`/`device`/`device_cables`), read in `zynthian_gui_touchkeypad_v5.__init__`
- [x] 1.2 Add `run_zynthian.sh` first-argument style selector with validation

## 2. Classic style (fallback baseline)

- [x] 2.1 Restore the original uneven 6-row/2-column grid, geometry formulas, and `"CTRL/PRSET"` label verbatim, gated behind `style == "classic"`

## 3. Standard style (real button grid)

- [x] 3.1 Replace the 6-row layout with a 4x5 grid in real-panel button order
- [x] 3.2 Fix `"CTRL/PRESET"` label typo (classic keeps the original typo; standard/device/device_cables get the fix)

## 4. Screen-area regression fixes

- [x] 4.1 Fix `panel_width` duplication bug: `zynthian_gui_config.set_touch_keypad()` now reads `panel_width`/`top_margin`/`button_height` from the keypad instance instead of recomputing them
- [x] 4.2 Widen `DISPLAY_WIDTH` in `run_zynthian.sh` for non-classic styles so the app screen area doesn't shrink relative to `classic` - **superseded for `standard` by 4.5**, kept for `device`/`device_cables`
- [x] 4.3 User validation: confirmed "Add Chain" no longer overlaps text/icons under `standard` with the widened window (width-only fix; height fix in 4.4 then reintroduced overlap from the other axis, see 4.5)
- [x] 4.4 Fix screen_height: was keyed off `top_margin == 0` (bucketing `standard` with `classic`'s deliberate bottom gap for its mega-row), leaving a black strip below the screen under `standard`. Keyed off `style == "classic"` initially - **superseded by 4.5**, since `standard` alone with a taller-but-same-width screen than before reintroduced icon/text overlap in "Add Chain" (icons scale to available cell size, text doesn't)
- [x] 4.5 Attempt: `standard` keeps classic's exact `screen_width`/`screen_height` by sharing classic's 5/6-height *ratio* formula too - **superseded by 4.6**, since that ratio only makes sense for classic's 6th mega-row filling the gap it creates; standard has no such row, so it left a plain black strip across the *full width*, under both the buttons and the screen
- [x] 4.6 Root fix: decoupled button height from screen height entirely. Buttons/screen for `standard` (like `device`/`device_cables`) now both just fill 100% of whatever height they're given (no ratio, no gap, by construction) - and `run_zynthian.sh` pre-shrinks `DISPLAY_HEIGHT` (in addition to the existing `DISPLAY_WIDTH` widening) for `standard` specifically, so that plain full-height formula still lands on classic's exact screen pixel size. `device`/`device_cables` still only get the width adjustment (unchanged, pending the real chassis render re-tune in 5.2-5.4)
- [x] 4.7 User validation: confirmed working correctly on a 5120x2160 display - no black bar, no icon/text overlap, buttons fill their column. `standard` style considered stable

## 5. Device style (chassis look)

- [x] 5.1 First pass: flat vector chassis background (chassis-coloured fill, bezel frame around screen, reserved top strip) - superseded, see 5.2
- [x] 5.2 Replace the flat vector background with `zynthian-webconf`'s official V5 mockup render, using its documented coordinates (screen at 731,254,800x480 - matches real screen resolution exactly; button hit-areas at 79,154 group offset, 122x113px grid step, 110x105 hit size; LED backlight circles at 133,208 group offset, r=45) - `draw_button_device()`/`draw_chassis_device()`
- [x] 5.3 Vendor the mockup render into the fork (`icons/v5_mockup/v5_render_zenital_mockup_1910.png`, from `github.com/zynthian/zynthian-webconf`, GPL-licensed)
- [x] 5.4 Geometry is now fixed/absolute for device/device_cables (not derived from a proportion guess at all) - `run_zynthian.sh` sets `DISPLAY_WIDTH`/`DISPLAY_HEIGHT` to the render's exact native size (1910x960, +160px top strip for device_cables)
- [x] 5.5 Button press feedback: yellow outline overlay (matching the mockup's own `push-buttons-outline` style) instead of the 2px-nudge classic/standard use, since the button face is baked into the image
- [x] 5.6 LED/active-state feedback: per-button backlight glow circle (`set_button_color` fills it from the wsled colour) instead of recolouring text, since there's no button text to recolour
- [x] 5.7 User validation round 1: screen positioning/chassis image confirmed good, but LED dots at the original r=45 (lifted straight from the mockup CSS without checking scale against our 110x105 button size) covered almost the entire button face, hiding the baked-in labels - every button just looked like a plain colored circle. First fix attempt: shrink to r=8 - **superseded by 5.9/5.10**
- [x] 5.9 Root cause: verified against actual PNG alpha data that most button label/glyph areas (BACK/NO's text, arrow glyphs, the panic "!" icon's plate) are transparent or semi-transparent cutouts meant to let a backlight colour show through from *behind* the image - matching the real mockup's own SVG order (`<svg id="leds">` drawn before `<image id="v5_render">`). Our LED dots were drawn *on top* of the image instead, defeating this entirely
- [x] 5.10 Fix: split button drawing into two passes - `draw_button_led()` (LED dot, drawn before the chassis image) and `draw_button_hitarea()` (invisible click target + press outline, drawn after). Bumped `V5_LED_RADIUS` back to 30 since the image now masks it down to the intended cutout shape. Verified via composited test renders: BACK/NO glows red, arrow glyphs glow yellow - matching real hardware exactly, answering the user's question about arrow/BACK colours
- [x] 5.11 Also fixed while investigating: white backing rectangle behind the whole render (not the canvas's default black bg), since the render's outer silhouette margin is *also* a transparent cutout meant to show a light page background (confirmed: "im Video ist der Hintergrund auch weiß") - previously showed as an unintended black halo/black icon-plate on our dark canvas
- [x] 5.12 User validation round 2: led-behind-image + white-backing confirmed visually closer, but arrows/BACK still grey (not yellow/red) and the circle shape left white gaps around wider text like "BACK" - root-caused to a `set_button_color` dedup bug (see design.md) and fixed by switching the dedup key to colour and the shape to a full-button rectangle
- [x] 5.13 User validation round 3 (device_cables): the white backing only covered the render's own area, leaving the cable-graphics strip above it on the default dark canvas colour - visibly inconsistent. Widened the white rect to the full canvas and switched cable-label text from white (invisible on white) to black
- [x] 5.14 Knob column (1658,125 group offset, r=52, 4 knobs) is part of the render image but has no interactive overlay yet - decorative-only was the agreed scope (user drives encoders via mouse-hover+scroll already), revisit only if requested - **implemented in change `touchkeypad-functional-knobs`**: hover/scroll/click overlays added, shipped in commit `f073af6` on the `fork` remote's `vangelis` branch
- [x] 5.15 User validation round 4: black line at the top edge of the render (transition to the white background above it) - root-caused to the explicit `create_line()` separator drawn in `draw_chassis_background()` (`fill=self.border_color` = black). Removed it entirely: both areas are the same white now, so a drawn seam only looked like an unwanted artifact
- [ ] 5.16 Performance check requested: is the right-hand screen's responsiveness (e.g. the mixer/level-meter bars) slightly delayed under `device`/`device_cables` compared to `classic`/`standard`? Code review found no likely culprit: the chassis image/LED/hit-area drawing all happens once in `__init__`, not repeatedly, and `device_cables`' only recurring timer (`refresh_connections()`, every 3s) is low-frequency and touches unrelated canvas items, not `root_frame`'s own rendering. The most plausible remaining explanation is simply the much larger window (1910x960/1120 vs standard's ~2133x800) giving the X server/compositor more total pixels to manage - not a bug in this code, if so. **Needs a live comparison to confirm before concluding anything** (e.g. `py-spy dump` while interacting under each style, or just eyeballing meter responsiveness back-to-back)

## 6. Device_cables style (live connections)

- [x] 6.1 Draw a labelled cable indicator per present audio capture port, using `zynautoconnect.get_audio_capture_ports()`
- [x] 6.2 Refresh indicators periodically (3s) to reflect plug/unplug
- [x] 6.3 Fix `lib_zyncore` stale-`None` regression: lazy-import `zynautoconnect` inside `draw_connections()` and defer the first call via `Canvas.after()` so it doesn't run before `lib_zyncore_init()` has completed (this bug affected *all* styles when first introduced, not just device_cables, since the eager top-level import ran regardless of style)
- [x] 6.4 User validation round 1 - **correction from user**: cables for the Korg Fisa Suprema *are* shown (likely 2 slots, probably its stereo L+R capture ports) - the earlier "no cables shown at all" report was wrong/premature. Real issue: slot spacing looks off. First fix attempt: even spacing based on actual item count instead of a fixed hypothetical max - **superseded by 6.8**, since even-across-the-width still had no relation to where the real jacks are drawn on the render
- [x] 6.8 Root fix attempt 1: measured the top port-icon row's actual x-positions directly off the vendored PNG (brightness clustering) - **corrected by user directly on disk** (my measurement was off; user supplied the accurate anchors: audio-out 330/477, audio-in 630/777, MIDI-in 945) plus new anchors for ports I hadn't covered (MIDI-thru 1130, MIDI-out 1320, LAN 1510, USB-3 1673, USB-2 1840, USB-B 2000)
- [x] 6.5 MIDI capture (in) ports were missing entirely. Added a `zynautoconnect.devices_in` query alongside audio-capture, drawn in `color_midi`, landing on the corrected MIDI-in anchor (945)
- [x] 6.6 Audio *output* wasn't shown as "cabled". Added a `zynautoconnect.get_hw_audio_dst_ports()` query, drawn in `color_alt2` (orange), landing on the corrected speaker-jack anchors (330/477)
- [x] 6.9 Added MIDI-*out* (via `zynautoconnect.devices_out`, same pattern as devices_in) landing on the MIDI-out anchor. First LAN attempt: `psutil` network-interface state (any reachable path, wired or WiFi) - **superseded by 6.14**, user clarified the simulation doesn't need to mirror this dev machine's actual network setup at all
- [x] 6.10 MIDI-THRU and USB 2.0/3.0/Type-B anchors exist (user-supplied) but are deliberately left unwired - no honest "is this actually connected" signal available yet without further investigation (unlike audio/MIDI-via-JACK or LAN), and a fake always-on cable would defeat the point of this style. Revisit if a real data source is identified
- [x] 6.11 Fixed a bug introduced while wiring LAN: referenced a nonexistent `zynthian_gui_config.color_default` - using `color_ml` instead
- [x] 6.12 Cables start 25px lower - **user correction**: this meant the *plug end* (closer to the device), not the outside/top end. Fixed: line top is back at y=0, the plug (circle) end now sits 25px past the cable-margin boundary, reaching further onto the device
- [x] 6.7 User question, needs research: can `zynthian-webconf` be run locally? Preliminary answer: yes in principle (standalone Tornado app, not installed here yet). Superseded in relevance by 6.14 (LAN no longer tries to detect real reachability at all) - USB-A specifically (6.10) is still unresolved and would still benefit from this research if pursued
- [x] 6.13 User validation round 2: found the port x-positions were measured against a 2383px-wide reference image, not our actual 1910px render - every anchor was proportionally too far right/spread out. Fixed: `_v5_port_x()` scales all anchors by `V5_IMAGE_SIZE[0]/_V5_PORT_REF_WIDTH` at load time. Sanity check: audio-in/MIDI-in now land almost exactly where the original automated pixel-cluster measurement had them, cross-validating both
- [x] 6.14 User clarification: LAN doesn't need to reflect this dev machine's real network state (which has both WiFi and a wired adapter active - irrelevant noise). Replaced the `psutil` scan with a single static "network" cable
- [x] 6.15 User observation: 3 MIDI-in + 2 MIDI-out devices are legitimately registered on this system (not a bug - this dev machine has many virtual/software MIDI endpoints), which drew as 3 and 2 separate fanned-out cables, cluttering a port that only has 1 physical jack on real hardware. Redesigned: at most `len(anchors)` cables are ever drawn per category (matching the real jack count); when more devices share one jack, that single cable gets a left-aligned, numbered stack of colour-chip labels (one row per device) instead of extra phantom cables
- [x] 6.16 In-chip text wrapping: labels wrap at ~30 chars (incl. enumeration prefix) inside their own chip instead of growing one giant single-line chip - `_wrap_cable_text()`
- [x] 6.17 User validation round 3: positive - MIDI cables update live correctly when the Fisa Suprema is powered on (a2jmidid's ALSA-seq hotplug support). **But** audio capture ports did not update at all, even after creating a chain - required an app restart to select the newly-available source. Root-caused to `run_zynthian.sh`'s `JACKD_OPTIONS` binding jackd's own ALSA backend directly to `hw:SUPREMA` at startup (no hotplug support), unlike MIDI's separate ALSA-seq bridging - this is a `zynthian-desktop-runtime` issue, not a `device_cables` bug, so it's tracked as its own change: see `fix-audio-hotplug-support`
- [ ] 6.18 Further user validation pending (in-chip wrapping - none of this has been seen running live yet)

## 7. Open feedback (from user screenshots, in progress)

- [x] 7.1 Point 1 (classic): confirmed good - readable text, no icon overlap, no wasted space; kept as the reference baseline
- [x] 7.2 Point 2 (standard): left-side buttons well arranged; unused space on the right + icon/text overlap in dialogs - root-caused to screen-area shrinkage, fixed in task 4.2, confirmed by user (4.3)
- [x] 7.3 Point 3 (device): inherits point 2's issues (same fix applies) + real device image from the demo-recording workflow was not used - sourced and implemented, see section 5
- [x] 7.4 Point 4 (standard, follow-up): black strip below the screen (buttons filled full height, screen only 5/6) - fixed in task 4.4, but reintroduced icon/text overlap
- [x] 7.5 Point 5 (standard, follow-up): overlap came back after 4.4; user's proposed direction (shrink the left buttons instead of resizing the screen, so it keeps classic's ratios) pointed at the right fix, refined in 4.6
- [x] 7.6 Point 6 (standard, follow-up): 4.5's fix introduced a full-width black bar under both buttons and screen; user's requirements (no black bar, screen matches classic's ratio, buttons fill the remaining space) pinned down the actual fix - implemented in 4.6
- [x] 7.7 Point 7 (standard, confirmation): works correctly on a 5120x2160 display - `standard` style closed out, see 4.7
- [ ] 7.8 Further points not yet received (device/device_cables still open, see section 5)
