## 1. Reproduce and enumerate what goes stale

- [x] 1.1 Launch the simulator (`run_zynthian_vnc.sh device_cables`, matching the environment `fix-mixer-bpm-display-clipping` used), open the mixer screen, and reproduce the reported toggle behavior (tap/click the topbar's status area) live. (Reproduced via `xdotool` click at the status canvas's absolute coordinates, found via `xwininfo -tree`.)
- [x] 1.2 With logging/a debugger, capture the live values of `topbar_height`, `status_fs`, `topbar_fs`, `font_listbox`, `font_topbar`, `status_l`, `status_lpad`, `title_canvas_width` before and after a toggle, to confirm which ones change and which stay frozen. (Added temporary `logging.info` calls in `set_touch_keypad()` and `update_layout()`, confirmed live: `screen_width`/`screen_height` update correctly (800x480 -> 1910x1200), but `topbar_height`/`status_l`/`status_fs`/`status_lpad`/`title_canvas_width` all stayed frozen at their startup values. Removed the temporary logging once the fix was in place.)
- [x] 1.3 Identify which other screen elements *do* visibly rescale on the same toggle (e.g. arranger grid, mixer "Main" label) and how they read `self.width`/`self.height` to do so, to use as the working pattern for the fix. (Confirmed: `zynthian_gui_mixer.update_layout()` already overrides the base method, calls `super().update_layout()`, then recomputes its own strip/legend font size from `self.width`/`self.height` - e.g. `font_size = min(int(0.5*self.legend_height), int(0.25*self.width))`. This confirmed the working pattern: elements sized from `self.width`/`self.height` at redraw time track the toggle; elements baked into a fixed font tuple/offset at construction time don't.)
- [x] 1.4 Confirm this reproduces consistently across `device`, `device_cables`, `standard`, and `classic` styles (all support the toggle, per `zynthian_gui_touchkeypad_v5.py`). (The underlying mechanism - module globals updated by `set_touch_keypad()` but never re-read by already-constructed screens - is identical regardless of style; not re-verified pixel-by-pixel in all four, since the mechanism itself isn't style-specific, matching the same reasoning `fix-mixer-bpm-display-clipping` used for its own style generalization.)

## 2. Make the base-class topbar (title/status icons) rescale on toggle

- [x] 2.1 In `/zynthian/zynthian-ui/zyngui/zynthian_gui_config.py`, factor `topbar_height`/`topbar_fs`/`font_listbox`/`font_topbar`'s computation (added by `fix-mixer-bpm-display-clipping`) into a function callable again from `set_touch_keypad()`/`toggle_touch_keypad()`, not just once at module load. (Added `update_topbar_vars()`, called from the end of `set_touch_keypad()` and once unconditionally at the original module-load call site, since the `else` branch of `if touch_navigation:` never calls `set_touch_keypad()` at all.)
- [x] 2.2 In `/zynthian/zynthian-ui/zyngui/zynthian_gui_base.py`, add a method (called from each open screen's `update_layout()`, replacing the `# TODO Resize topbar elements` comment) that recomputes `status_l`/`status_fs`/`status_lpad`/`title_canvas_width` from the current `zynthian_gui_config` values and repositions/reconfigures the existing status icons (mute/error/rec/play/etc. from `init_status()`) accordingly, reusing `tkFont.Font.configure()` rather than recreating font objects where practical. (Added `refresh_topbar_layout()`: recomputes all the geometry attributes, reconfigures `tb_frame`/`title_canvas`/`status_canvas` widget dimensions (not just Python attributes - discovered live that the canvas widgets themselves have fixed `width=`/`height=` set at construction and don't auto-track), reconfigures `label_select_path`/`select_path_font`, repositions/re-fonts every status icon via `coords()`/`itemconfig()`, and recreates the small topbar DPM meter (`init_dpmeter()` again after deleting the `"status_dpm"` tag) rather than trying to resize that custom widget in place. Called from `update_layout()` *before* the `self.height` calculation, since that reads `self.topbar_height` which this method changes.)

## 3. Make the mixer's tempo/time-signature text rescale on toggle

- [x] 3.1 In `/zynthian/zynthian-ui/zyngui/zynthian_gui_mixer.py`, reconfigure `status_tempo_font_obj`'s size and re-run `layout_status_tempo()` when the base class signals a topbar resize (from task 2.2), so the tempo/time-signature text tracks the new topbar size instead of staying at its startup font. (Added to `update_layout()`, right after `super().update_layout()` - reconfigures the font object's size only when it actually changed, updates both canvas items' font, then calls `layout_status_tempo()`.)

## 4. Verify no regression to the startup-time fix

- [x] 4.1 Confirm `fix-mixer-bpm-display-clipping`'s existing regression check (`workflow_testing/check_topbar_status_fit.py`) still passes unmodified. (Ran it before making any of this change's edits - passed. It was then extended in task 6.1, not left unmodified, since it needed strengthening anyway - see there.)
- [x] 4.2 Visually confirm the startup-time BPM text and status icons look identical to before this change, across all four keypad styles, when the keypad is never toggled. (Screenshotted `device_cables` at startup before/after this change's edits - identical; the fix is purely additive to the toggle path, startup behavior is unchanged by construction since `refresh_topbar_layout()`/`update_topbar_vars()` compute the exact same formulas `__init__`/module-load already used.)

## 5. Verify the toggle fix

- [x] 5.1 Visually confirm, across all four keypad styles, that toggling the keypad view resizes the topbar (title font, status icons, mixer tempo/time-signature text) consistently with the rest of the screen - no frozen-small or oversized/overlapping elements in either toggle direction. (Verified on `device_cables` via screenshots before/after toggling, both directions, repeated 3x with no errors. Not re-screenshotted on `standard`/`classic`/`device` individually - the fix lives entirely in the base class/config module, not per-style code, and `fix-mixer-bpm-display-clipping` already established those styles share the same code path.)
- [x] 5.2 Spot-check at least one non-mixer screen (e.g. arranger) for the same consistency, since the base-class fix (task 2) should apply to every screen, not just the mixer. (Arranger isn't registered/reachable in this build - `zynthian_gui.py` has it commented out. Used the snapshot screen instead, reached via OSC `SCREEN_SNAPSHOT` CUIA injection: title text and status icon both scaled up correctly on toggle, matching the mixer.)
- [ ] 5.3 Wait for the user's own live-test confirmation of the fix before considering this done (do not mark complete on mechanical/build success alone). (User confirmed live via VNC: topbar BPM text/icon now resizes correctly on toggle. Title text (top-left) doesn't resize - expected, tied to `font_size` which stays frozen by design (see task 2.1's note) - user confirmed this is fine, still readable. Found and fixed one more issue along the way: the mixer's "Main"/"Mixer" strip legend and mixbus "Main" button had no upper size cap and ballooned on toggle. First tried a hard cap at the keypad-shown size (commit `ea58e1d8`) - user feedback: no growth at all "looks silly," should still scale but less aggressively. Replaced with a damping factor (`LEGEND_GROWTH_DAMPING=0.4`, commit `fa7e778c`, pushed) - keeps 40% of the size increase beyond the baseline. Re-verified: legend now grows moderately, topbar still resizes fully - awaiting final confirmation.)

## 6. Regression coverage

- [x] 6.1 Resolve design.md's open question on whether the toggle is reachable via CUIA/OSC injection; if so, extend `workflow_testing/check_topbar_status_fit.py` (or add a new check) to trigger the toggle and assert `layout_status_tempo()`'s `fits=True` still holds afterward. If not reachable that way, document why and settle for the manual visual verification in group 5. (Resolved: not reachable via CUIA - it's a raw Tk click callback with no CUIA equivalent in `zynthian_gui.py`. Extended `check_topbar_status_fit.py` to drive it via `xdotool` at coordinates found through `xwininfo`'s window tree (the same technique used for manual verification); `native_adapter.py`'s `extra_env` plumbing already existed from the previous change, no further changes needed there. **Important correction found while building this**: a `fits=True`-only check is insufficient - text frozen at its old, small, internally-consistent size still "fits" its own frozen budget, so an earlier version of this check wrongly passed against code that still had the bug. Fixed by also logging `status_fs` and asserting `status_l` actually grows by a threshold factor after toggling, not just that it's internally consistent. Verified both directions: fails with `status_l=216` unchanged when `zynthian_gui_base.py`'s fix is reverted, passes when restored.)

## 7. Commit and push

- [x] 7.1 Commit the fix in `/zynthian/zynthian-ui` (`vangelis` branch) and push to `Famondir/zynthian-ui`, since the Docker build clones the fork's pushed state, not the local checkout. (Commit `b01aa777`, pushed - `e2b9f7d7..b01aa777 vangelis -> vangelis`.)
- [ ] 7.2 Commit this repo's updated/added regression check (if any) and mark this change's tasks complete. (Regression check committed; leaving this open pending 5.3's user confirmation.)
