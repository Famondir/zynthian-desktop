## 1. Reproduce and confirm root cause

- [x] 1.1 Launch the Docker/simulated Zynthian (`run_zynthian_docker.sh` or VNC) with the `device`/`device_cables` keypad style, open the mixer screen, and confirm the BPM text is clipped as in the reported screenshot. (A `run_zynthian_vnc.sh device_cables` instance was already running live in this environment, matching the reported screenshot.)
- [x] 1.2 Add temporary logging (or use a debugger) in `/zynthian/zynthian-ui/zyngui/zynthian_gui_config.py` and `zynthian_gui_base.py` to print the live values of `display_width`, `screen_width`, `font_size`, `topbar_fs`, `status_l`, and `status_fs` in this environment. (Read straight from the running process's env (`/proc/<pid>/environ`: `DISPLAY_WIDTH=1910`, `DISPLAY_HEIGHT=1200`) and traced `zynthian_gui_config.py`'s init order against them - see design.md Context for the full derivation.)
- [x] 1.3 Measure the actual rendered pixel width of the tempo string (e.g. via `tkFont.Font(font=("forkawesome", status_fs)).measure("120.0 bpm")`) and compare it against the available room (`status_l - status_fs*3.5`) to confirm the overflow amount. (Measured live against the running X display: 244px vs. 90px budget at the buggy font size, 93px vs. 167px at the correct size - see design.md.)
- [x] 1.4 Check whether the same clipping reproduces under `classic`/`standard` keypad styles or only `device`/`device_cables`, and note the result in design.md's Open Questions. (Traced: same ordering bug exists for all styles, but `classic`/`standard` scale `screen_height` proportionally with `display_height` so the mismatch is ~20% vs. `device`/`device_cables`'s ~150%, explaining why only the latter was reported.)
- [x] 1.5 Confirm whether `forkawesome` is actually available as an installed font in the Docker image for plain digit/letter glyphs, or whether Tk is silently substituting a fallback font with different metrics. (Confirmed installed via `fc-list`; ruled out as a contributing factor.)

## 2. Fix font sizing to match the actual content area

- [x] 2.1 In `/zynthian/zynthian-ui/zyngui/zynthian_gui_config.py`, change `font_size`'s derivation (and anything computed from it that affects topbar layout, e.g. `topbar_fs`) to key off `screen_width` instead of `display_width` wherever they differ, so hardware behavior (`display_width == screen_width`) is unchanged. (Refined during implementation: moving/rebasing `font_size` itself broke the touch keypad's own button-label sizing, which needs `font_size` early and display-based - see design.md's Decisions. The actual fix moves only `topbar_height`/`topbar_fs`/`font_listbox`/`font_topbar` to after `set_touch_keypad()`, leaving `font_size` untouched.)
- [x] 2.2 Re-verify `status_l`'s derivation in `/zynthian/zynthian-ui/zyngui/zynthian_gui_base.py` still uses `screen_width`-derived `topbar_width`, so both quantities are consistent. (Confirmed unchanged and already correctly timed - `zynthian_gui_base.__init__` runs after `zynthian_gui_config` module init completes.)

## 3. Make the status text width self-correcting

- [x] 3.1 In `/zynthian/zynthian-ui/zyngui/zynthian_gui_mixer.py`, replace the hand-tuned `status_fs * 3.5` / `status_fs * 8.5` offsets with an offset computed from the actual measured text width of the current tempo/time-signature strings (e.g. `tkFont.Font(...).measure(text)`), recomputed whenever the displayed text changes (`update` calls around lines ~1535-1537). (Added `layout_status_tempo()`, called from `set_tempo`, `set_bpb`, and the beat-change branch in `refresh_status`; it measures both strings with a shared `tkFont.Font` and repositions the tempo text at `status_l - right_margin` and the time-signature text immediately to its left, closing the "no gap between them" cosmetic quirk noticed during verification as a side effect.)
- [x] 3.2 Confirm the offset calculation accounts for the longest realistic tempo/time-signature strings this UI can show (e.g. 3-digit BPM, larger time signatures like `16/16`), not just the `"120.0 bpm"` default. (Since the offset is now measured from the live rendered text rather than assumed, this is automatic; spot-checked `"999.9 bpm"` and `"16 | 16/16"` at the corrected font size - both measure well within the `status_l` budget.)

## 4. Verify the fix

- [x] 4.1 Rebuild/relaunch the Docker or native simulator with `device`/`device_cables` keypad style and visually confirm the full BPM and time-signature text now renders without clipping. (Screenshot confirms `1 | 4/4  120.0 bpm` renders in full with a clean gap.)
- [x] 4.2 Visually confirm no regression under `classic`/`standard` styles and on a plain (no-keypad) layout. (Screenshots confirm `classic` and `standard` both render the full status text correctly and the rest of each layout - button grid, screen area, DPM meter - is unaffected. Plain/no-keypad layout isn't reachable in this project's desktop environment (`ZYNTHIAN_WIRING_LAYOUT=TOUCH_ONLY` always enables a keypad style), but is provably unaffected by inspection: when `touch_navigation` is falsy, `set_touch_keypad()` is never called, so `screen_width`/`screen_height` never diverge from `display_width`/`display_height` and every changed calculation is a no-op - matching real hardware, where the two are already always equal.)
- [ ] 4.3 Wait for the user's own live-test confirmation of the fix before considering this done (do not mark complete on mechanical/build success alone). (In progress: user confirmed the clipping itself is fixed, then found two further issues from live use - see group 7.)

## 5. Regression coverage

- [x] 5.1 Add a `workflow_testing` screenshot-based check (or extend an existing mixer-screen smoke test) that asserts the topbar status text region has no unexpected clipping under the `device`/`device_cables` style, per `python3 -m workflow_testing.run_all`'s module docstring for how to wire in a new check. (Implemented as `workflow_testing/check_topbar_status_fit.py` - reads a ground-truth log line from `layout_status_tempo()` rather than screenshotting pixels, see design.md's Decisions for why. Required adding an `extra_env` parameter to `native_adapter.launch()`. Verified live in both directions: fails with `timesig_left_edge=-79` when the config.py fix is reverted, passes when restored.)

## 6. Commit and push

- [x] 6.1 Commit the fix in `/zynthian/zynthian-ui` (`vangelis` branch) and push to `Famondir/zynthian-ui`, since the Docker build clones the fork's pushed state, not the local checkout. (Commit `fe196fbd`, pushed - `ebbe8bcc..fe196fbd vangelis -> vangelis`. A follow-up margin fix from group 7 needs its own commit/push once 7's tasks settle.)
- [ ] 6.2 Commit this repo's `workflow_testing` regression check (if added) and mark this change's tasks complete. (Commit `c9a00ca`, pushed. Left open: still gated on 4.3/7.)

## 7. Findings from live user testing (after 4.1-4.2's automated verification)

- [x] 7.1 Fix: tempo text now overlaps the topbar's permanent CPU-load/update-status icon (green ↻, `zynthian_gui_base.py`'s `status_error` item, always anchored at `status_l` - not an error-only indicator despite the name, see its `refresh_status()`). `layout_status_tempo()`'s `right_margin` was `0.3*status_fs`, nowhere near enough to clear a full icon glyph; bumped to `1.3*status_fs`, matching `status_mute`'s own established reservation convention for the same icon. Measured the actual glyph widths (`↻`=19px, `♥`=17px at status_fs=14) to confirm 1.3x clears them with room to spare. Verified live: green icon now sits with a clear gap after "bpm".
- [ ] 7.2 Decision needed (not yet fixed): toggling the touch keypad view at runtime (short tap/click on the status area - `zynthian_gui_base.py`'s `status_short_touch_action` → `zynthian_gui_config.toggle_touch_keypad()`) does not rescale the topbar - `update_layout()` has a literal `# TODO Resize topbar elements` and never recomputes `topbar_height`/`status_fs`/`font_topbar`/`status_l`, which are one-time per-instance/per-module values from startup. Meanwhile other elements (e.g. the arranger/pattern grid's `4/4` and pattern-number labels, the mixbus "Main" label) *do* rescale on toggle, growing large enough to overlap each other. This is a pre-existing gap (the TODO predates this change) exposed by, not caused by, this fix - fixing it means wiring real-time topbar/status resizing into `update_layout()` across every screen, a materially bigger and more architectural change than the reported bug. Asked user whether to scope this into the current change, defer it as tracked follow-up, or something else.
